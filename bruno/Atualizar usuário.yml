info:
  name: Atualizar usuário
  type: http
  seq: 21

http:
  method: PATCH
  url: http://localhost:3000/users/1
  body:
    type: json
    data: |-
      {
        "nome": "Emilly Atualizada"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
