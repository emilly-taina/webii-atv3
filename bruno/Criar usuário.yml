info:
  name: Criar usuário
  type: http
  seq: 20

http:
  method: POST
  url: http://localhost:3000/users
  body:
    type: json
    data: |-
      {
        "nome": "Usuario Teste",
        "email": "usuario12.teste@gmail.com",
        "papel": "PROFESSOR"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
