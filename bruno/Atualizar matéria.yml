info:
  name: Atualizar matéria
  type: http
  seq: 26

http:
  method: PATCH
  url: http://localhost:3000/subjects/1
  body:
    type: json
    data: |-
      {
        "nome": "Programação Web Atualizada",
        "professorId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
