info:
  name: Criar matéria teste
  type: http
  seq: 29

http:
  method: POST
  url: http://localhost:3000/subjects
  body:
    type: json
    data: |
      {
        "nome": "Programação Web 2 - Teste",
        "professorId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
